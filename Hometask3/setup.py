from setuptools import setup, find_packages

setup(
    name="snapshot",
    packages=find_packages(),
    scripts=[],
    version="0.1",
    author="Andrei Shvedau",
    description="Application for system monitoring",
)
